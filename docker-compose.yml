services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tradelog:latest
    container_name: tradelog_app
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./database:/var/www/html/database
      - ./storage:/var/www/html/storage
    environment:
      - APP_NAME=Tradelog
      - APP_KEY=base64:x4osgOdx6iDQ1+ViZh89QAXP+Oh78fc51tGx69S0vvU=
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_URL=http://localhost:8080
      - LOG_LEVEL=error
      - DB_CONNECTION=sqlite
      - DB_DATABASE=/var/www/html/database/database.sqlite
      - SESSION_DRIVER=database
      - QUEUE_CONNECTION=database
    networks:
      - tradelog_network

  # Optional: MySQL database (uncomment if you prefer MySQL over SQLite)
  # mysql:
  #   image: mysql:8.0
  #   container_name: tradelog_mysql
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_DATABASE: tradelog
  #     MYSQL_ROOT_PASSWORD: root_password
  #     MYSQL_USER: tradelog_user
  #     MYSQL_PASSWORD: tradelog_password
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   networks:
  #     - tradelog_network

networks:
  tradelog_network:
    driver: bridge

# Uncomment if using MySQL
# volumes:
#   mysql_data:
